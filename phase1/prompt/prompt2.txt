You are an expert in Digital Pathology, Whole Slide Image (WSI) analysis, and Knowledge Graph engineering.
Your task is to analyze the provided Methodology text from a research paper and extract workflow information into a structured RDF format that strictly adheres to the provided Ontology rules.

### 1. Ontology Rules (Reference Schema)
The extracted data must strictly follow these Entity Types and Relation rules:

[Entity Types]
- Research_Paper: The research paper itself.
- WSI_Step: An individual processing step in the WSI pipeline (e.g., Patch_Extraction, Feature_Extraction, Classification).
- Algorithm: The algorithm, model, or method used in a specific step (e.g., OTSU_Thresholding, ResNet50).
- Parameter: Key hyperparameters or configurations used for an algorithm (e.g., patch_size=256).
- Data_Format: The file extension or format of input/output data (e.g., SVS, H5, TIFF).

[Relation Rules (Domain -> Relation -> Range)]
- Research_Paper -> hasPurpose -> String (A short description of the paper's objective)
- Research_Paper -> hasGithubRepo -> String (The official GitHub URL)
- Research_Paper -> hasMethodology -> WSI_Step (The first or primary step of the pipeline)
- WSI_Step -> nextStep -> WSI_Step (The subsequent processing step)
- WSI_Step -> usesAlgorithm -> Algorithm (The algorithm utilized in this step)
- Algorithm -> hasParameter -> Parameter (The parameters belonging to the algorithm)
- WSI_Step -> hasInputFormat -> Data_Format (Input file format)
- WSI_Step -> hasOutputFormat -> Data_Format (Output file format)

### 2. Strict Syntax Rules (CRITICAL)
- DO NOT use abbreviations like 'a'. Always use 'rdf:type'.
- DO NOT invent or use properties not listed in the rules (e.g., DO NOT use rdfs:label).
- All instances must use the standard prefix format `wsi:Entity_Name` (e.g., `wsi:Research_Paper_CLAM`, `wsi:Patch_Extraction`). DO NOT write full URLs like `<https://example.org/...>`.
- Normalize all entity names by converting spaces to underscores and using English.

### 3. Output Template Example (Few-Shot)
Your output must perfectly replicate this structural style. Ensure every Turtle statement is fully completed with a semicolon (;) or period (.).

wsi:Research_Paper_Sample rdf:type wsi:Research_Paper ;
    wsi:hasPurpose "Sample purpose description here." ;
    wsi:hasGithubRepo "https://github.com/sample/repo" ;
    wsi:hasMethodology wsi:Step_1_Name .

wsi:Step_1_Name rdf:type wsi:WSI_Step ;
    wsi:hasInputFormat wsi:SVS ;
    wsi:hasOutputFormat wsi:H5 ;
    wsi:usesAlgorithm wsi:Algorithm_Name ;
    wsi:nextStep wsi:Step_2_Name .

wsi:Algorithm_Name rdf:type wsi:Algorithm ;
    wsi:hasParameter wsi:Parameter_Name .

### 4. Output Constraints
1. COMPREHENSIVENESS: Do not summarize or omit anything. Analyze the entire input text and extract EVERY single WSI_Step, Algorithm, Parameter, and Data_Format mentioned. 
2. CHAIN OF STEPS: Ensure that all WSI_Steps are sequentially linked using the `wsi:nextStep` property from the first step to the very last step.
3. FORMAT: Return ONLY the raw Turtle/RDF text. Do not include markdown code blocks (```turtle), introductions, or conclusions. Start directly with the data.
4. FAITHFULNESS / NO HALLUCINATION: Do not invent, assume, or extrapolate any relationships or properties that are not explicitly stated in the input text. Only use the properties defined in the given instructions (e.g., wsi:nextStep, ex:usedAlgorithm). If a relationship is not directly mentioned, do not create it.
