You are an expert in Digital Pathology, Whole Slide Image (WSI) analysis, and Knowledge Graph engineering.
Your task is to analyze the provided Abstract and Methodology text from a research paper and extract workflow information into a structured RDF format.

### 1. Strict Syntax & Structural Rules (CRITICAL)
- DO NOT use abbreviations like 'a'. Always use 'rdf:type'.
- DO NOT invent or use properties not listed in the rules (e.g., DO NOT use rdfs:label).
- DO NOT connect a Parameter entity directly to a Step entity. It MUST always be connected to an Algorithm entity.
- Every Application of a Step MUST have exactly one relation to a shared WSI processing step.
- Every WSI processing step MUST have exactly one relation pointing to WSI processing stage.
- The Research Paper entity MUST link to EVERY Application of a Step, and to the first one via a relation for the start of the Methodology.
- Name WSI processing step as shared concepts. Name the Application of a Step distinctly as local occurrences using the framework's name.
- All instances must use the standard prefix format `wsi:Entity_Name`. DO NOT write full URLs like `<https://example.org/...>`.
- Normalize all entity names by converting spaces to underscores and using English.

### 2. Output Constraints
1. SCOPE THEN COMPLETENESS: Extract only the authors' final proposed workflow (the operational pipeline they introduce), not baselines, rejected alternatives, ablation variants, or related-work methods. Within that pipeline, do not summarize or omit any WSI procesisn step, Application of a Step, Algorithm, Parameter, or Data.
2. SHARED VS Application: Reuse shared WSI processing step concept names across papers. Put paper-specific pipeline details only on Application of a Step.
3. CHAIN OF APPLICATIONS: Link all Application of a Step nodes with the next Application from first to last. Within-paper order is preserved by it, not by stage.
4. PROVENANCE: The Research Paper entity must reference every Application of a Step.
5. STAGE ASSIGNMENT: Assign every WSI processing step to exactly one canonical stage. Multiple Application of a Step nodes may point to different WSI processing steps that share the same stage.
6. FORMAT: Return ONLY the raw Turtle/RDF text. Do not include markdown code blocks (```turtle), introductions, or conclusions. Start directly with the data.
7. INDIVIDUAL TYPE DECLARATION: Explicitly declare the `rdf:type` for EVERY single extracted instance.
8. FAITHFULNESS / NO HALLUCINATION: Do not invent, assume, or extrapolate any relationships or properties that are not explicitly stated in the input text. Only use the properties defined in the given instructions. Stage assignment and mapping are allowed judgments based on the step's role.
